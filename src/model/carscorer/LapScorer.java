package model.carscorer;

import model.entity.Car;

/**
 * Gives points for completing more laps
 */
public class LapScorer implements CarScorer {
  @Override
  public double scoreCar(Car car) {
    //if (car is touching a checkpoint)
    //    give car points
    //    destroy this checkpoint
    //    place the next checkpoint
    return 0;
  }
}
